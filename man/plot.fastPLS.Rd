% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{plot.fastPLS}
\alias{plot.fastPLS}
\title{Plot PLS latent scores}
\usage{
\method{plot}{fastPLS}(
    x,
    comps = c(1L, 2L),
    groups = NULL,
    score.set = c("auto", "train", "test"),
    ellipse = FALSE,
    ellipse.type = c("confidence", "hotelling"),
    conf = 0.95,
    ...
)
}
\arguments{
\item{x}{A fitted \code{fastPLS} object.}

\item{comps}{Two component indices.}

\item{groups}{Optional grouping vector used for point fills and grouped
ellipses.}

\item{score.set}{Plot \code{train} scores, \code{test} scores, or use
    \code{auto} to select
stored training scores before test scores.}

\item{ellipse}{Draw confidence ellipses when \code{TRUE}.}

\item{ellipse.type}{Use \code{confidence} or \code{hotelling} ellipses.}

\item{conf}{Confidence level.}

\item{...}{Additional arguments passed to \code{plot()}.}
}
\value{
Invisibly returns the plotted score matrix.
}
\description{
Draws a two-component score plot for a fitted \code{fastPLS} object. Optional
ellipses are computed either as data confidence ellipses or Hotelling T2
score ellipses. Axis labels include predictor-space variance explained when
it was requested during model fitting.
}
\examples{
X <- as.matrix(iris[, seq_len(4)])
fit <- pls(X, iris$Species, ncomp = 2, fit = TRUE,
            return_variance = TRUE, seed = 1)
plot(fit, groups = iris$Species, ellipse = TRUE)
}
