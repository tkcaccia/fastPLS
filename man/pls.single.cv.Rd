% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{pls.single.cv}
\alias{pls.single.cv}
\title{Single cross-validation for PLS component optimization}
\usage{
pls.single.cv(
  Xdata,
  Ydata,
  ncomp = 2,
  constrain = NULL,
  scaling = c("centering", "autoscaling", "none"),
  method = c("simpls", "plssvd", "opls", "kernelpls"),
  backend = NULL,
  n.cores = NULL,
  seed = 1L,
  kfold = 10,
  north = 1L,
  kernel = c("linear", "rbf", "poly"),
  gamma = NULL,
  degree = 3L,
  coef0 = 1,
  classifier = c("argmax", "lda"),
  fit = TRUE,
  bycol = FALSE,
  selection = "auto",
  ...
)
}
\arguments{
\item{Xdata}{Predictor matrix.}

\item{Ydata}{Response. Use a numeric vector/matrix for regression or
factor/character class labels for classification.}

\item{ncomp}{Positive integer component count or vector of counts. Repeated
values are removed. PLS-SVD, the SIMPLS-family estimator, and kernel PLS
cap the returned path at the corresponding numerical rank and report the
effective counts.}

\item{constrain}{Optional grouping vector for grouped cross-validation. It
must have one value per sample. Samples with the same value are assigned to
the same fold, so all rows from the same patient, subject, batch, or
technical replicate stay together in training or test data. When \code{NULL},
each sample is treated as its own group.}

\item{scaling}{One of \code{centering}, \code{autoscaling}, or \code{none}.}

\item{method}{One or more of \code{simpls}, \code{plssvd}, \code{opls}, or
\code{kernelpls}. Multiple values are treated as a tuning grid.
\code{simpls} denotes the fastPLS SIMPLS-family estimator; an eligible
bounded-block route is approximate rather than classical de Jong SIMPLS.}

\item{backend}{Implementation backend: \code{cpu}, \code{cuda}, or
\code{metal}. Multiple values are treated as a tuning grid. Metal requires
float32 input and uses fixed CPU/Metal operation splitting with CPU fold
orchestration and prediction. When omitted,
\code{options(backend = ...)} defines the session default. Every requested
backend must be available;
unavailable accelerator entries raise an error instead of using CPU.}

\item{n.cores}{Number of CPU cores requested for supported BLAS/OpenMP host
operations. An explicit value takes precedence over
\code{options(n.cores = ...)}. CUDA and Metal device parallelism is managed by
their native runtimes; \code{n.cores} applies only to their host-side stages.}

\item{seed}{Random seed used for fold assignment and randomized SVD steps.}

\item{kfold}{Number of folds, or \code{"loocv"} for leave-one-out
cross-validation. When \code{constrain} is supplied, LOOCV means
leave-one-constraint-group-out: samples sharing the same constraint value
are always held out together and are never split across training and test.
A numeric value at least as large as the number of groups has the same
leave-one-group-out interpretation.}

\item{north}{Number of orthogonal components removed by OPLS.
The predictive count \code{ncomp} must fit within the predictor rank remaining
after filtering. Requests above this bound raise an error, including
within cross-validation folds, rather than fitting a numerical null
direction. Orthogonal components are not counted as predictive components.}

\item{kernel}{Kernel type for kernel PLS: \code{linear}, \code{rbf}, or
\code{poly}.}

\item{gamma}{Kernel scale. Defaults internally to \code{1 / ncol(Xdata)}. For
\code{method = "kernelpls"}, multiple values are treated as a tuning grid.}

\item{degree}{Polynomial kernel degree.}

\item{coef0}{Polynomial kernel offset.}

\item{classifier}{Classification rule for factor responses: \code{"argmax"} or
latent-space \code{"lda"}. Multiple values are treated as a tuning grid.}

\item{fit}{Fit one additional model on the full dataset and return its
fitted values (\code{Yfit}) and training \code{R2Y} path. The default is \code{TRUE} for
backward compatibility. Set to \code{FALSE} to skip this extra full-data fit;
held-out cross-validated \code{Q2Y} and \code{RMSD} are still calculated.
Selecting \code{R2Y} overrides \code{fit = FALSE} because fitted responses are
required for that criterion.}

\item{bycol}{For matrix-valued regression responses, calculate response-wise
metrics in the returned \code{metrics} list. The default \code{FALSE} returns only
aggregate metrics.}

\item{selection}{Metric used to select settings. \code{"auto"} uses accuracy for
classification and RMSD for regression. Classification also supports
\code{"balanced_accuracy"}, \code{"lift_accuracy"}, \code{"macro_precision"},
\code{"macro_recall"}, \code{"macro_f1"}, \code{"kappa"}, \code{"R2Y"}, and \code{"Q2Y"}.
Regression also supports \code{"R2Y"}, \code{"Q2Y"}, \code{"RMSD"}, \code{"MAE"},
\code{"MAPE_percent"}, \code{"RPD"}, \code{"Pearson_r"}, and
\code{"Spearman_r"}. R2Y uses a model fitted to the complete supplied dataset
and therefore forces \code{fit = TRUE}; as a training criterion, it is usually
less suitable for complexity selection than a held-out criterion. Q2Y
uses out-of-fold predictions and each fold's training-response mean. The
remaining metrics use the aggregate definitions in \code{\link[=evaluate]{evaluate()}} on the
out-of-fold predictions. RMSD, MAE, and MAPE_percent are
minimized; all other criteria are maximized. Classification R2Y and Q2Y
operate on dummy-coded responses, not decoded labels. Incompatible
task/metric combinations raise an error before fitting. The former names
\code{"r2"} and \code{"q2"} are rejected as ambiguous.}

\item{...}{Optional SVD tuning controls forwarded to the selected backend.
Use the same compact names documented in \code{\link[=fastsvd]{fastsvd()}}, such as
\code{oversample} and \code{power}. Vector values are included in the tuning grid.}
}
\value{
A list describing the cross-validation run and selected model.
\code{metrics$cross_validated} contains complete \code{evaluate()} results for each
requested component count and \code{metrics$fitted} contains the corresponding
full-data fit results when \code{fit = TRUE}. \code{metrics$definitions} records the
exact R2Y and Q2Y denominator conventions. Other fields are:
\itemize{
\item \code{best_ncomp}: number of components selected by the chosen metric.
\item \code{best_index}: position of \code{best_ncomp} in the tested component grid.
\item \code{selection_metric}: metric used for optimization. With \code{"auto"},
classification uses accuracy and regression uses the default prediction
error rule.
\item \code{best_metric_name} and \code{best_metric_value}: name and value of the
metric at the selected component count.
\item \code{Q2Y}: held-out cross-validated Q2; every held-out fold is centered
on its corresponding fold-training response mean. For factor responses,
this is dummy-response PLS-DA Q2 using fold-training class proportions and
is not classification accuracy. Values are named by component count.
\item \code{accuracy}: held-out decoded-label accuracy for factor responses,
named by component count.
\item \code{balanced_accuracy}: held-out mean class recall for factor responses,
named by component count.
\item \code{RMSD}: held-out root mean squared deviation for regression. It is
\code{NA} for classification. Values are named by component count.
\item \code{Yfit}: fitted values from the full-data model when \code{fit = TRUE}.
\item \code{R2Y}: training-set explained-variance path from a model fitted on
the full dataset when \code{fit = TRUE}; otherwise \code{NA}. For factor
responses, this is calculated on the dummy-coded PLS-DA response scores,
not on the decoded class labels.
\item \code{fold}: fold assignment used for each sample.
\item \code{pred}: decoded cross-validated predictions when predictions are
stored.
\item \code{Ypred}: raw prediction array when score predictions are stored.
\item \code{metrics}: complete \code{evaluate()} outputs. \code{cross_validated} contains
one result per requested component count from held-out predictions and
\code{fitted} contains full-data fit results when \code{fit = TRUE}. For multivariate
regression, response-wise metrics are included only when \code{bycol = TRUE}.
\item \code{selection_metrics}: compact per-component metric table used for
component selection by the CV backend.
\item \code{best_parameters}: compact list containing only \code{ncomp} plus the
arguments that were actually optimized, for example \code{classifier} when
\code{classifier = c("argmax", "lda")}.
\item \code{tuning_config}: relevant selected configuration used for the run.
Irrelevant classifier- or method-specific defaults are omitted; for
example, controls belonging to an unselected classifier are omitted.
\item \code{tuning_summary} and \code{tuning_metrics}: tables for all tested
configurations when more than one predictive configuration is supplied.
\item The returned object can be passed as the first argument to \code{\link[=pls]{pls()}} to
refit the selected model on the full training data and predict new samples.
}
}
\description{
Performs grouped k-fold or leave-one-out cross-validation over candidate
component counts and, when vector-valued predictive arguments are supplied,
over a compact hyperparameter grid. Selection uses a task-appropriate metric
returned by \code{\link[=evaluate]{evaluate()}} or the fitted-response R2Y path.
}
\details{
For LDA classification, each training fold uses only the classes
represented in that fold and maps predictions back to the original factor
levels. A class absent from a fold's training data cannot be predicted in
that fold; its held-out observations remain in the reported metrics. When
otherwise identical \code{"argmax"} and \code{"lda"} configurations are tuned
together, the PLS fit and fold projection are calculated once; each
classifier still receives its own predictions, metric path, and selected
component count. For sufficiently tall SIMPLS classification problems,
full-data predictor and class moments are calculated once and each fold's
training moments are obtained by subtracting its held-out contribution.
The fold-specific centering, scaling, PLS fit, LDA fit, and predictions
remain independent. For regression, a fold that estimates fewer latent
components than requested uses its available component prefix. Higher
requested prefixes repeat the last estimable prediction; if no component is
estimable, the prediction is the fold-training response mean. The returned
path still contains one result for every requested component count.
}
\examples{
idx <- c(seq_len(12), 51:62, 101:112)
X <- as.matrix(iris[idx, seq_len(4)])
y <- factor(iris[idx, 5])
opt <- pls.single.cv(X, y,
    ncomp = seq_len(2), kfold = 3, method = "simpls",
    backend = "cpu", seed = 1
)
opt$best_ncomp
opt_kernel <- pls.single.cv(X, y,
    ncomp = seq_len(2), kfold = 3,
    method = "kernelpls", backend = "cpu",
    kernel = c("linear", "rbf"),
    gamma = c(0.1, 1), seed = 1
)
opt_kernel$best_parameters
}
