% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{pls.double.cv}
\alias{pls.double.cv}
\title{Nested cross-validation for PLS}
\usage{
pls.double.cv(
    Xdata,
    Ydata,
    ncomp = 2,
    constrain = seq_len(nrow(Xdata)),
    scaling = c("centering", "autoscaling", "none"),
    method = c("simpls", "plssvd", "opls", "kernelpls"),
    backend = NULL,
    svd.method = c("rsvd", "irlba"),
    seed = 1L,
    perm.test = FALSE,
    times = 100,
    runn = 1,
    kfold_inner = 10,
    kfold_outer = 10,
    north = 1L,
    kernel = c("linear", "rbf", "poly"),
    gamma = NULL,
    degree = 3L,
    coef0 = 1,
    classifier = c("argmax", "lda"),
    lda_ridge = NULL,
    bycol = FALSE,
    xprod = NULL,
    selection_metric = "auto",
    ...
)
}
\arguments{
\item{Xdata}{Predictor matrix.}

\item{Ydata}{Response (numeric or factor).}

\item{ncomp}{Number of components (scalar or vector).}

\item{constrain}{Grouping vector for grouped cross-validation. It must have
one value per sample. Samples with the same value are assigned to the same
fold, so all rows from the same patient, subject, batch, or technical
replicate stay together in training or test data. The default
\code{seq_len(nrow(Xdata))} treats every sample as an independent group.}

\item{scaling}{One of \code{centering}, \code{autoscaling}, or \code{none}.}

\item{method}{One or more of \code{simpls}, \code{plssvd}, \code{opls}, or
\code{kernelpls}. Multiple values are tuned in the inner loop.}

\item{backend}{Implementation backend: \code{cpu}, \code{cuda}, or
\code{metal}. Multiple values are tuned in the inner loop.}

\item{svd.method}{SVD algorithm family. \code{rsvd} uses the native fastPLS
randomized SVD for the selected backend, and \code{irlba} uses the bundled
CPU iterative backend.}

\item{seed}{Random seed used for outer/inner fold assignment and randomized
SVD steps.}

\item{perm.test}{Run a nested-CV permutation test. Independent rows are
permuted individually. With repeated \code{constrain} values, complete blocks
are exchanged only among groups with the same number of rows, preserving
group sizes, within-group response structure, and class frequencies.
Outer/inner folds and randomized-solver seeds are fixed across observed
and null fits. The statistic is the median outer-CV value of the metric
used for inner model selection.}

\item{times}{Number of requested permutations. The corrected Monte Carlo
p-value is \code{(b + 1) / (B + 1)}, using the upper tail for metrics where
larger is better and the lower tail for losses such as RMSD. \code{B} counts
successful null fits only; failed fits are reported. A grouped test
requires at least two constraint groups of equal size.}

\item{runn}{Number of repeated runs.}

\item{kfold_inner}{Inner-fold count, or \code{"loocv"} to leave out one
constraint group at a time inside each outer training set.}

\item{kfold_outer}{Outer-fold count, or \code{"loocv"} to leave out one
constraint group at a time in the outer loop. In both loops, samples
sharing the same constraint value are never split across training and test.}

\item{north}{Number of orthogonal components removed by OPLS.}

\item{kernel}{Kernel type for kernel PLS: \code{linear}, \code{rbf}, or \code{poly}.}

\item{gamma}{Kernel scale. Defaults internally to \code{1 / ncol(Xdata)}. For
\code{method = "kernelpls"}, multiple values are tuned in the inner loop.}

\item{degree}{Polynomial kernel degree.}

\item{coef0}{Polynomial kernel offset.}

\item{classifier}{Classification decision rule. \code{argmax} keeps the
standard PLS-DA response-score argmax. \code{lda} fits a regularized LDA
classifier on the PLS latent scores.}

\item{lda_ridge}{Deprecated compatibility argument. It is ignored and emits
a warning when supplied. PLS-LDA uses a fixed, scale-normalized Cholesky
fallback sequence rather than a user-tuned ridge.}

\item{bycol}{For matrix-valued regression responses, calculate response-wise
metrics in the returned \code{metrics} list. The default \code{FALSE} returns only
aggregate metrics.}

\item{xprod}{Use the matrix-free cross-product route where available for
inner component optimization. \code{NULL} applies fastPLS defaults.}

\item{selection_metric}{Metric used by inner CV and by the permutation test.
Use \code{"auto"} (accuracy for classification and RMSD for regression),
\code{"accuracy"}, \code{"balanced_accuracy"}, \code{"r2"}, \code{"q2"}, or \code{"rmsd"}.
For numeric responses, \code{"r2"} is an observed-mean held-out endpoint and
\code{"q2"} uses fold-training response means. Training \code{R2Y} is descriptive
and is never optimized. Classification can use accuracy, balanced
accuracy, or dummy-response Q2, but not training R2. Balanced accuracy
gives each observed class equal weight.}

\item{...}{Optional SVD tuning controls forwarded to the selected backend.
Use the same compact names documented in \code{\link[=fastsvd]{fastsvd()}}, such as
\code{oversample}, \code{power}, \code{svds_tol}, \code{work}, \code{maxit}, \code{tol}, \code{eps},
and \code{svtol}. Vector values are tuned in the inner loop.}
}
\value{
A list with the following elements. \code{metrics$cross_validated}
contains one complete \code{evaluate()} result per repeated outer-CV run, and
\code{metrics$aggregate} evaluates the final vote-aggregated or averaged
prediction. \code{metrics$definitions} records the exact R2Y and Q2Y
denominator conventions.
\itemize{
\item \code{results}: list with one element per repeated run. Each run stores
\code{Ypred}/\code{pred}, the outer \code{fold} assignment, \code{best_ncomp} selected in
each outer fold, fold-level \code{best_parameters}, the complete inner-CV
objects in \code{inner}, run-level \code{metric_name} and \code{metric_value}, and the
default \code{backend} and \code{method}.
\item \code{Ypred}: final cross-validated predictions. For classification, repeated
runs are combined by voting; for regression, numeric predictions are
averaged across runs.
\item \code{Q2Y}: one outer cross-validated Q2 value per repeated run. Numeric
responses use each outer fold's training-response mean. For factor
responses this is the mean outer-fold dummy-response PLS-DA Q2 using
outer-training class proportions and is not classification accuracy.
\item \code{R2Y}: one training-fit R2 value per repeated run, averaged across the
selected outer-fold models.
\item \code{RMSD}: one held-out RMSD value per repeated run for numeric responses;
\code{NA} for classification.
\item \code{metric_name}: held-out metric used for each repeated run.
\item \code{bcomp}: most frequently selected component count across outer folds and
repeated runs.
\item \code{backend}, \code{method}: default backend and PLS method supplied to the call.
If vector-valued methods or backends are tuned, selected fold-level values
are stored in \code{results[[run]]$best_parameters}.
\item \code{selection_metric}: criterion used by the inner CV loop.
\item \code{acc_tot}: classification-only text summary of correctly classified
samples and percentage accuracy.
\item \code{conf}: classification-only confusion matrix printed as counts and
column percentages.
\item \code{vote_counts}: classification-only vote-count matrix with one row per
sample and one column per class.
\item \code{accuracy}: classification-only decoded-label accuracy, one value per
repeated run.
\item \code{balanced_accuracy}: classification-only unweighted mean of class recalls,
one value per repeated run.
\item \code{medianR2Y}, \code{CI95R2Y}, \code{medianQ2Y}, \code{CI95Q2Y}, \code{medianRMSD},
\code{CI95RMSD}: repeated-run summaries returned only when \code{runn > 1}.
\item \code{permutation_metric}, \code{permutation_observed}, and \code{permutation_sampled}:
metric name, observed median, and permuted medians returned when
\code{perm.test = TRUE}. \code{Q2Ysampled} is retained only when Q2 is the selected
permutation metric.
\item \code{p.value}: permutation-test p-value returned when \code{perm.test = TRUE}.
\item \code{permutation_unit}, \code{permutation_group_sizes_preserved},
\code{permutation_class_frequencies_preserved}, \code{permutation_folds},
\code{permutation_solver_seed}, \code{permutation_requested},
\code{permutation_completed}, \code{permutation_failed}, and \code{permutation_errors}:
the exchangeability contract and null-fit audit.
}
}
\description{
Performs nested grouped cross-validation with an outer loop for unbiased
performance estimation and an inner loop for component and hyperparameter
selection. Constraint groups are respected in both loops so related samples
remain in the same fold.
}
\examples{
idx <- c(seq_len(10), 51:60, 101:110)
X <- as.matrix(iris[idx, seq_len(4)])
y <- factor(iris[idx, 5])
dcv <- pls.double.cv(X, y, ncomp = seq_len(2), runn = 1, kfold_inner = 2,
                        kfold_outer = 2, method = "simpls", backend = "cpu",
                        svd.method = "rsvd", seed = 1)
names(dcv)
}
