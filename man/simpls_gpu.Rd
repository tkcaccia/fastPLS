% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{simpls_gpu}
\alias{simpls_gpu}
\alias{simpls_flash_gpu}
\title{GPU-native SIMPLS fit}
\usage{
simpls_gpu(
  Xtrain,
  Ytrain,
  Xtest = NULL,
  Ytest = NULL,
  ncomp = 2,
  scaling = c("centering", "autoscaling", "none"),
  rsvd_oversample = 10L,
  rsvd_power = 1L,
  svds_tol = 0,
  seed = 1L,
  fit = FALSE,
  proj = FALSE,
  gpu_device_state = TRUE,
  gpu_qr = FALSE,
  gpu_eig = FALSE,
  gpu_qless_qr = TRUE,
  gpu_finalize_threshold = 32L,
  fast_block = 1L,
  fast_center_t = FALSE,
  fast_reorth_v = FALSE,
  fast_incremental = TRUE,
  fast_inc_iters = 2L,
  fast_defl_cache = TRUE,
  gaussian_y = FALSE,
  gaussian_y_dim = NULL,
  gaussian_y_seed = seed
)

simpls_flash_gpu(
  Xtrain,
  Ytrain,
  Xtest = NULL,
  Ytest = NULL,
  ncomp = 2,
  scaling = c("centering", "autoscaling", "none"),
  rsvd_oversample = 10L,
  rsvd_power = 1L,
  svds_tol = 0,
  seed = 1L,
  fit = FALSE,
  proj = FALSE,
  gpu_device_state = TRUE,
  gpu_qr = FALSE,
  gpu_eig = FALSE,
  gpu_qless_qr = TRUE,
  gpu_finalize_threshold = 32L,
  fast_block = 1L,
  fast_center_t = FALSE,
  fast_reorth_v = FALSE,
  fast_incremental = TRUE,
  fast_inc_iters = 2L,
  fast_defl_cache = TRUE
)
}
\arguments{
\item{Xtrain}{Numeric training predictor matrix.}

\item{Ytrain}{Training response (numeric or factor).}

\item{Xtest}{Optional test predictor matrix.}

\item{Ytest}{Optional observed response used to compute `Q2Y`.}

\item{ncomp}{Number of components (scalar or vector).}

\item{scaling}{One of `"centering"`, `"autoscaling"`, `"none"`.}

\item{rsvd_oversample}{RSVD oversampling.}

\item{rsvd_power}{RSVD power iterations.}

\item{svds_tol}{Tolerance placeholder passed through to the backend.}

\item{seed}{Random seed.}

\item{fit}{Return fitted values and `R2Y` when `TRUE`.}

\item{proj}{Return projected `Ttest` when `TRUE`.}

\item{gpu_device_state}{Keep selected SIMPLS workspaces resident on the GPU when `TRUE`.}

\item{gpu_qr}{Use GPU QR finalization when available.}

\item{gpu_eig}{Use GPU eigensolver finalization when available.}

\item{gpu_qless_qr}{Use the q-less GPU QR path when available.}

\item{gpu_finalize_threshold}{Component threshold controlling GPU-side finalization.}

\item{fast_block}{Refresh block size for the fastPLS `simpls` core.}

\item{fast_center_t}{Deprecated and ignored.}

\item{fast_reorth_v}{Deprecated and ignored.}

\item{fast_incremental}{Deprecated and ignored.}

\item{fast_inc_iters}{Deprecated and ignored.}

\item{fast_defl_cache}{Deprecated and ignored.}

\item{gaussian_y}{Logical; when `TRUE`, fit to a Gaussian random response
sketch and decode predictions back to the original response scale or class
labels. The default is `FALSE`.}

\item{gaussian_y_dim}{Number of Gaussian response dimensions. When `NULL`,
the default is `min(ncol(Xtrain), 100)`.}

\item{gaussian_y_seed}{Random seed used to generate the Gaussian response
sketch.}
}
\value{
A `fastPLS` object.
}
\description{
Uses a CUDA-oriented `simpls` engine that keeps the training
matrices and deflated cross-covariance resident on device throughout the fit.
}
