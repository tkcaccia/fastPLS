% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{pls}
\alias{pls}
\title{Partial Least Squares with selectable model family and backend}
\usage{
pls(
  Xtrain,
  Ytrain,
  Xtest = NULL,
  Ytest = NULL,
  ncomp = 2,
  scaling = c("centering", "autoscaling", "none"),
  method = c("simpls", "plssvd", "opls", "kernelpls"),
  svd.method = c("rsvd", "irlba"),
  classifier = c("argmax", "lda", "cknn"),
  lda_ridge = 1e-08,
  k = 10L,
  tau = 0.2,
  alpha = 0.75,
  top_m = 20L,
  cknn_memory = c("auto", "standard", "blocked", "streaming"),
  fit = FALSE,
  return_variance = TRUE,
  return_loadings = FALSE,
  proj = FALSE,
  perm.test = FALSE,
  times = 100,
  backend = c("cpu", "cuda", "metal"),
  north = 1L,
  kernel = c("linear", "rbf", "poly"),
  gamma = NULL,
  degree = 3L,
  coef0 = 1,
  ...
)
}
\arguments{
\item{Xtrain}{Numeric training predictor matrix. Alternatively, a result
returned by \code{single.pls.cv()}. In that case \code{pls()} refits the selected
model on the full cross-validation training set and predicts \code{Xtest} using
\code{best_ncomp} and the selected tuning settings.}

\item{Ytrain}{Training response (numeric or factor). When \code{Xtrain} is a
\code{single.pls.cv()} result, the second positional argument may be used as
\code{Xtest}.}

\item{Xtest}{Optional test predictor matrix.}

\item{Ytest}{Optional test response for \code{Q2Y}.}

\item{ncomp}{Number of components (scalar or vector).}

\item{scaling}{One of \code{centering}, \code{autoscaling}, or \code{none}.}

\item{method}{One of \code{simpls}, \code{plssvd}, \code{opls}, or \code{kernelpls}.
\code{simpls} uses the fastPLS accelerated SIMPLS core.}

\item{svd.method}{SVD algorithm family for compiled CPU fits: \code{irlba} or
\code{rsvd}. Use CUDA or Metal backends for native GPU
fits where available.}



\item{classifier}{Classification decision rule. \code{argmax} keeps the
standard PLS-DA response-score argmax. \code{lda} fits an LDA classifier on
the PLS latent scores. \code{cknn} is the compact name for the PLS-score
candidate-kNN classifier: class centroids in PLS score space choose
candidate classes, then every sample is reranked by within-candidate kNN in
the same PLS score space. The compiled implementation is selected automatically from
\code{backend}: C++ for \code{cpu}, CUDA for \code{cuda}, and Metal for \code{metal}
where available.}

\item{lda_ridge}{Relative diagonal ridge added to the pooled LDA covariance.}

\item{k}{Number of same-class PLS-score neighbours used by
the candidate-kNN classifier.}

\item{tau}{Positive temperature used to smooth the neighbour
similarities in candidate-kNN scoring.}

\item{alpha}{Weight of the centroid/prototype candidate score
added to the local kNN score.}

\item{top_m}{Number of centroid-ranked candidate classes passed to
the kNN reranker.}

\item{cknn_memory}{Memory strategy for \code{classifier = "cknn"}.
\code{standard} uses the historical one-pass candidate-kNN path.
\code{blocked} predicts test samples in blocks, reducing test-side
latent-score memory. \code{streaming} additionally builds the training
candidate-score cache in blocks for scalar component counts. \code{auto}
chooses a memory-aware strategy from the data size.}

\item{fit}{Return fitted values and \code{R2Y} when \code{TRUE}.}

\item{return_variance}{Compute predictor-space latent-variable variance
explained. Set to \code{FALSE} for timing/memory benchmarks that do not need
plotting variance metadata.}

\item{return_loadings}{Compute and store predictor loadings \code{P}. The default
is \code{FALSE} because \code{P} is mainly used for interpretation/loading plots and
is not needed for prediction, VIP, or the benchmark pipelines.}

\item{proj}{Return projected \code{Ttest} when \code{TRUE}.}

\item{perm.test}{Run permutation test.}

\item{times}{Number of permutations.}

\item{backend}{Implementation backend: \code{cpu} for compiled CPU, \code{cuda}
for CUDA-native fitting, or experimental \code{metal} for Apple Metal
randomized-SVD/GEMM acceleration.}

\item{north}{Number of orthogonal components removed by OPLS.}

\item{kernel}{Kernel type for kernel PLS: \code{linear}, \code{rbf}, or \code{poly}.}

\item{gamma}{Kernel scale. Defaults internally to \code{1 / ncol(Xtrain)}.}

\item{degree}{Polynomial kernel degree.}

\item{coef0}{Polynomial kernel offset.}

\item{...}{Optional SVD tuning controls forwarded to the selected backend.
Use the same compact names documented in \code{\link[=fastsvd]{fastsvd()}}, such as
\code{oversample}, \code{power}, \code{svds_tol}, \code{work}, \code{maxit}, \code{tol}, \code{eps},
\code{svtol}, and \code{seed}.}
}
\value{
A \code{fastPLS} object. The object is a list whose fields depend on the
selected method, backend, classifier, and whether test data or optional
summaries were requested. Common fields are:
\itemize{
\item \code{P}: predictor loadings, with one column per latent component. By default this is an empty \code{0 x 0} matrix; set \code{return_loadings = TRUE} to compute it for interpretation or loading plots.
\item \code{Q}: response loadings or response-side latent coefficients.
\item \code{R}: predictor weights/rotations used to project new samples into the PLS latent space.
\item \code{Ttrain}: training latent scores. This is returned when the backend stores scores or when they are needed for fitted values, classifiers, variance summaries, or compact prediction.
\item \code{C_latent}, \code{W_latent}: low-rank latent prediction factors used by PLSSVD-style compact prediction when a full coefficient array is avoided.
\item \code{B}: regression coefficient matrix or coefficient array, when stored. For vector-valued \code{ncomp}, a three-dimensional array may contain the coefficient path for all requested component counts.
\item \code{B_stored}: logical flag indicating whether the full \code{B} coefficients were stored.
\item \code{compact_prediction}: logical flag indicating that prediction can use compact low-rank factors instead of a full \code{p x q} coefficient matrix.
\item \code{mX}, \code{vX}: training predictor centering and scaling values. \code{vX} is one when no scaling is applied.
\item \code{mY}: response centering values for regression or dummy-coded PLS-DA.
\item \code{p}, \code{m}: number of predictor and response columns used internally.
\item \code{ncomp}: requested/effective component count vector. For PLSSVD this may be capped by the numerical rank of the response.
\item \code{pls_method}: fitted PLS family, such as \code{"plssvd"} or \code{"simpls"}.
\item \code{xprod_default}: logical flag indicating whether fastPLS selected the matrix-free cross-product route for the fit.
\item \code{predict_latent_ok}: logical flag indicating whether latent-space prediction is available for \code{predict()}.
\item \code{predict_backend}, \code{flash_svd}, \code{flash_svd_backend}, \code{flash_svd_mode}, \code{flash_block_size}: prediction/backend metadata for the compact streamed prediction path, when used.
\item \code{classification}: logical flag indicating whether \code{Ytrain} was a factor and the model was fitted as PLS-DA.
\item \code{lev}: factor levels used for classification.
\item \code{classification_rule}: classification head used for factor responses: \code{"argmax"}, \code{"lda"}, or \code{"cknn"} internally resolved to the selected backend.
\item \code{lda_backend}: backend used by the latent-space LDA classifier, when \code{classifier = "lda"}.
\item \code{Yfit}: fitted training responses or fitted class labels, returned when \code{fit = TRUE}.
\item \code{R2Y}: training-set coefficient of determination path when \code{fit = TRUE}; otherwise \code{NA} placeholders may be returned for compatibility.
\item \code{Ypred}: predictions for \code{Xtest}, returned only when \code{Xtest} is supplied to \code{pls()}. For classification this contains predicted factor labels; for regression it contains numeric predictions.
\item \code{Ypred_index}: integer class indices for classification predictions, when available.
\item \code{Ttest}: test-set latent scores, returned when \code{proj = TRUE}.
\item \code{Q2Y}: test-set Q2 for numeric \code{Ytest}, or dummy-response PLS-DA Q2 for factor \code{Ytest}, returned when response scores are available.
\item \code{accuracy}: decoded-label accuracy for factor \code{Ytest}, returned when classification predictions are available.
\item \code{pval}: permutation-test p-values by component, returned when \code{perm.test = TRUE}.
\item \code{variance}, \code{variance_explained}, \code{cumulative_variance_explained}, \code{variance_total}, \code{variance_basis}: predictor-space variance summaries returned when \code{return_variance = TRUE}.
\item \code{x_variance}, \code{x_variance_explained}, \code{x_cumulative_variance_explained}, \code{x_variance_total}: aliases of the predictor-space variance summaries.
\item \code{inner_model}: fitted inner PLS model used by OPLS.
\item \code{W_orth}, \code{P_orth}, \code{north}, \code{opls_engine}, \code{xprod_mode}, \code{gpu_resident}: OPLS-specific orthogonal-component and backend metadata.
\item \code{kernel}, \code{kernel_engine}, \code{kernel_linear_direct}: kernelPLS-specific kernel settings and execution metadata.
}
}
\description{
Fits PLSSVD, SIMPLS, OPLS, or kernel PLS models for regression or
classification using a selected CPU, CUDA, or Metal backend. The fitted model
can include predictions for held-out samples, latent scores, fitted values,
variance summaries, and optional classification heads.
}
\examples{
X <- as.matrix(mtcars[, c("disp", "hp", "wt", "qsec")])
y <- mtcars$mpg
fit <- pls(X, y, ncomp = 2, method = "simpls", backend = "cpu",
           svd.method = "rsvd", return_variance = FALSE)
head(predict(fit, X)$Ypred)

cv <- single.pls.cv(X, y, ncomp = 1:2, kfold = 3, method = "simpls",
                    backend = "cpu", svd.method = "rsvd", seed = 1)
fit_cv <- pls(cv, Xtest = X, return_variance = FALSE)
cv$best_ncomp
head(fit_cv$Ypred)
}
