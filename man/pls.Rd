% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{pls}
\alias{pls}
\title{Partial Least Squares with selectable model family and backend}
\usage{
pls(
  Xtrain,
  Ytrain,
  Xtest = NULL,
  Ytest = NULL,
  ncomp = 2,
  scaling = c("centering", "autoscaling", "none"),
  method = c("simpls", "plssvd", "opls", "kernelpls"),
  classifier = c("argmax", "lda"),
  fit = FALSE,
  bycol = FALSE,
  return_variance = TRUE,
  return_loadings = FALSE,
  proj = FALSE,
  perm.test = FALSE,
  times = 100,
  backend = NULL,
  n.cores = NULL,
  north = 1L,
  kernel = c("linear", "rbf", "poly"),
  gamma = NULL,
  degree = 3L,
  coef0 = 1,
  ...
)
}
\arguments{
\item{Xtrain}{Numeric training predictor matrix or a \code{float::float32}
predictor matrix for the supported float32 route.}

\item{Ytrain}{Training response. Use a numeric vector/matrix for regression
or factor/character class labels for classification.}

\item{Xtest}{Optional test predictor matrix.}

\item{Ytest}{Optional test response for independent-test \code{Q2Y}, whose
denominator uses the training-response mean. Classification labels may
contain classes absent from the training data; such classes cannot be
predicted and count as classification errors.}

\item{ncomp}{Positive integer component count or vector of counts. Repeated
values are removed. When a direct PLS-LDA fit reaches its numerical rank,
all requested positions are retained and positions beyond that rank repeat
the last estimable prediction and discriminant scores. The fitted object
reports both requested and effective component counts.}

\item{scaling}{One of \code{centering}, \code{autoscaling}, or \code{none}.}

\item{method}{One of \code{simpls}, \code{plssvd}, \code{opls}, or
\code{kernelpls}.
\code{simpls} uses the fastPLS accelerated SIMPLS-family core. Bounded-block
execution is approximate and is not classical de Jong SIMPLS.}

\item{classifier}{Classification decision rule. \code{argmax} keeps the
standard PLS-DA response-score argmax. \code{lda} fits a regularized LDA
classifier on the PLS latent scores.}

\item{fit}{Return fitted values, training scores, and \code{R2Y} when
\code{TRUE}. The default \code{FALSE} keeps only the compact state required for
prediction and avoids materializing training-only outputs.}

\item{bycol}{For matrix-valued regression responses, calculate response-wise
metrics in \code{metrics}. The default \code{FALSE} returns only aggregate metrics.}

\item{return_variance}{Compute predictor-space latent-variable variance
explained. Set to \code{FALSE} for timing/memory benchmarks that do not need
plotting variance metadata.}

\item{return_loadings}{Compute and store predictor loadings \code{P}. The default
is \code{FALSE} because most prediction workflows only need the projection
weights and response-side coefficients.}

\item{proj}{Return projected \code{Ttest} when \code{TRUE}.}

\item{perm.test}{Run a single-split permutation test when \code{Xtest} and
\code{Ytest} are supplied. Because \code{pls()} has no grouping argument, the
exchangeability unit is one training row. Training rows are permuted, the
model is refitted with the same randomized-solver seed, and the permuted
test-set \code{Q2Y} path is compared with the observed path.}

\item{times}{Number of requested permutations. For each component, \code{pls()}
returns \code{(b + 1) / (B + 1)}, where \code{b} is the number of successful null
fits with \code{Q2Y} at least as large as observed and \code{B} is the number of
successful null fits. Failed fits are excluded from \code{B} and reported.}

\item{backend}{Implementation backend: \code{cpu} for compiled CPU,
\code{cuda} for CUDA-native fitting, or \code{metal} for operation-split
float32 CPU/Metal fitting on Apple silicon. When omitted,
\code{options(backend = ...)}
defines the session default. Selecting unavailable CUDA or Metal
support raises an error without a CPU fallback.}

\item{n.cores}{Number of CPU cores requested for supported BLAS/OpenMP host
operations. An explicit value takes precedence over
\code{options(n.cores = ...)}. CUDA and Metal device parallelism is managed by
their native runtimes; \code{n.cores} applies only to their host-side stages.}

\item{north}{Number of orthogonal components removed by OPLS.
The predictive count \code{ncomp} must fit within the predictor rank remaining
after filtering. Direct PLS-LDA fits retain larger requested positions by
repeating the last estimable result; other OPLS routes reject an unavailable
predictive direction. Orthogonal components are not counted as predictive
components.}

\item{kernel}{Kernel type for kernel PLS: \code{linear}, \code{rbf}, or
\code{poly}.}

\item{gamma}{Kernel scale. Defaults internally to \code{1 / ncol(Xtrain)}.}

\item{degree}{Polynomial kernel degree.}

\item{coef0}{Polynomial kernel offset.}

\item{...}{Optional SVD tuning controls forwarded to the selected backend.
Use the same compact names documented in \code{\link[=fastsvd]{fastsvd()}}, such as
\code{oversample}, \code{power}, and \code{seed}.}
}
\value{
A \code{fastPLS} object. The object is a list whose fields depend on the
selected method, backend, classifier, and whether test data or optional
summaries were requested. \code{metrics} is a list of complete \code{evaluate()}
results, organized as \code{metrics$fitted} and \code{metrics$test}, with one element
per requested component count. Common fields are:
\itemize{
\item \code{P}: predictor loadings, with one column per latent component when
\code{return_loadings = TRUE}; otherwise an empty matrix is returned.
\item \code{Q}: response loadings or response-side latent coefficients.
\item \code{R}: predictor weights/rotations used to project new samples into the PLS
latent space.
\item \code{Ttrain}: training latent scores when \code{fit = TRUE}. With
\code{fit = FALSE}, classification routes retain only the compact state needed
for prediction or LDA fitting and do not return the full training-score matrix.
Compiled cross-validation continues to return its documented score outputs.
\item \code{C_latent}, \code{W_latent}: low-rank latent prediction factors used by
PLS-SVD-style compact prediction when a full coefficient array is
avoided.
\item \code{B}: regression coefficient matrix or coefficient array, when stored.
For vector-valued \code{ncomp}, a three-dimensional array may contain the
coefficient path for all requested component counts.
\item \code{mX}, \code{vX}: training predictor centering and scaling values. \code{vX} is one
when no scaling is applied.
\item \code{mY}: response centering values for regression or dummy-coded PLS-DA.
\item \code{lev}: factor levels used for classification.
\item \code{Yfit}: fitted training responses or fitted class labels, returned when
\code{fit = TRUE}.
\item \code{R2Y}: training-set coefficient of determination path when \code{fit = TRUE};
otherwise \code{NA} placeholders may be returned for compatibility. Elements
are named by component count, for example \code{"ncomp=2"}. For PLS-DA this
is a dummy-response quantity, not classification accuracy.
\item \code{requested_ncomp}: component positions requested by the caller. For
rank-limited PLS-LDA fits, every position is retained in fitted and predicted
outputs.
\item \code{effective_ncomp}: number of estimable response-associated directions
used for each requested component prefix. Rank-limited PLS-LDA paths repeat
the last estimable prediction and discriminant scores. If a regression
response is constant, this is zero; fitted and new-data predictions then equal
the training-response mean, coefficients are zero, and \code{R2Y} is \code{NA}.
\item \code{Ypred}: predictions for \code{Xtest}, returned only when \code{Xtest} is supplied
to \code{pls()}. For classification this contains predicted factor labels; for
regression it contains numeric predictions.
\item \code{Ypred_index}: integer class indices for classification predictions, when
available.
\item \code{Ttest}: test-set latent scores, returned when \code{proj = TRUE}.
\item \code{Q2Y}: independent-test Q2 whose denominator centers each response on
its corresponding training-response mean before aggregating sums of
squares. For factor \code{Ytest}, this is dummy-response PLS-DA Q2 relative
to training class proportions, not classification accuracy. It is
returned when response scores are available. Elements are named by
component count.
\item \code{accuracy}: decoded-label accuracy for factor \code{Ytest}, returned when
classification predictions are available. Elements are named by component
count.
\item \code{metrics}: complete \code{evaluate()} outputs. \code{definitions} records the exact
R2Y and Q2Y denominator conventions. \code{fitted} evaluates \code{Yfit}
against \code{Ytrain}; \code{test} evaluates \code{Ypred} against \code{Ytest}. For
multivariate regression, response-wise metrics are included only when
\code{bycol = TRUE}. \code{metrics$permutation} stores permutation metrics and
p-values when \code{perm.test = TRUE}.
\item \code{pval}: corrected Monte Carlo permutation-test p-values by component,
returned when \code{perm.test = TRUE}.
\item \code{permutation}: long-format permutation table, returned when
\code{perm.test = TRUE}, with observed and permuted \code{R2}/\code{Q2} values and the
permutation correlation used by \code{plot.permutation()}.
\item \code{permutation_unit}, \code{permutation_group_sizes_preserved},
\code{permutation_class_frequencies_preserved}, \code{permutation_folds},
\code{permutation_solver_seed}, \code{permutation_requested},
\code{permutation_completed}, \code{permutation_failed}, and \code{permutation_errors}:
the permutation contract and null-fit audit.
\item \code{variance}, \code{variance_explained}, \code{cumulative_variance_explained},
\code{variance_total}, \code{variance_basis}: predictor-space variance summaries
returned when \code{return_variance = TRUE}.
\item \code{x_variance}, \code{x_variance_explained},
\code{x_cumulative_variance_explained}, \code{x_variance_total}: aliases of the
predictor-space variance summaries.
\item \code{inner_model}: fitted inner PLS model used by OPLS.
\item \code{W_orth}, \code{P_orth}, \code{north}, \code{opls_engine}, \code{xprod_mode},
\code{gpu_resident}: OPLS-specific orthogonal-component and backend metadata.
\item \code{kernel}, \code{kernel_engine}, \code{kernel_linear_direct}: kernelPLS-specific
kernel settings and execution metadata.
\item \code{diagnostics}: numerical solver diagnostics. For rSVD this records the
structural-check status, finiteness, requested and effective component
counts and randomized controls. A route that invokes the case-audited
CPU decomposition also records its residual audit, strengthened retries,
and any deterministic recovery; other routes state explicitly that a
case audit is unavailable. Panel evidence is reported separately and is
not interpreted as general-use certification. SIMPLS-family fits also record
whether the active approximate route uses a component-wise oversampled
sketch or an eligible CPU/CUDA/Metal candidate block, together with the active execution
optimizations.
}

Function settings and backend bookkeeping, such as the component grid and
resolved classifier backend, are retained internally for prediction and
plotting but are not shown as public output fields.
}
\description{
Fits PLS-SVD, a SIMPLS-family estimator, OPLS, or kernel PLS models for
regression or classification using a selected CPU, CUDA, or operation-split
Apple Metal backend. The fitted
model can include predictions for held-out samples, latent scores, fitted
values, variance summaries, and optional classification heads.
}
\details{
The CPU backend uses Apple Accelerate on macOS, prefers OpenBLAS
when available on Linux and Windows, and otherwise uses the numerical
libraries supplied by R. Use \code{\link[=fastPLS_blas]{fastPLS_blas()}} to report the library chosen
at compilation. \code{options(n.cores = n)} requests CPU threads when supported
by that library; additional threads are not guaranteed to improve every
fit.

Base R numeric matrices use float64. Supplying \code{float::float32} predictors
or numeric responses requests float32 execution without silent promotion.
CUDA supports float32 and float64. The Apple Metal route requires float32
and divides work between CPU code and persistent Metal workspaces. Consecutive
products in the implicit transpose cross-covariance operator share one Metal
command buffer and synchronize before the CPU response-centering correction.
Unsupported backend and precision combinations stop rather than silently
falling back to CPU.

\code{method = "simpls"} selects the fastPLS SIMPLS-family estimator. Its
component-wise route retains the classical sequential orthogonalization and
deflation structure. An eligible route may instead consume a bounded block of
candidates computed from one deflated state; this is an approximate
SIMPLS-family estimator, not classical de Jong SIMPLS. Classification can use
response-score argmax or LDA on latent scores. PLS-SVD classification cannot
return more than one fewer component than the number of response classes. The
requested model family is never silently replaced by another family.

All public PLS routes use randomized SVD. It is an approximate solver, and
\code{diagnostics} records the effective controls and structural checks for the
fitted route. Compare repeated seeds or an independent high-accuracy fit
when results are close to a decision boundary or the data are strongly
ill-conditioned. Explicit \code{oversample}, \code{power}, and \code{seed} values supplied
through \code{...} override the automatic controls.

LDA uses a regularized pooled within-class covariance calculation with
Cholesky solves. Regularization increases through a fixed internal sequence
only when factorization fails and is not user-tuned.
}
\examples{
X <- as.matrix(mtcars[, c("disp", "hp", "wt", "qsec")])
y <- mtcars$mpg
fit <- pls(X, y,
    ncomp = 2, method = "simpls", backend = "cpu",
    return_variance = FALSE
)
head(predict(fit, X)$Ypred)

}
